define(["views/ListView","jquery","angular"], 
    function( ListView,jquery, angular) {
    var obj = function(){
        var listview = new ListView();
        return listview;
    };,
    return obj;
});